# ============================================================
# Outline Docker Compose Configuration
# ============================================================
# Copy this file to .env and customize for your environment
# Then run: docker-compose up -d
# ============================================================

# REQUIRED: Generate with: openssl rand -hex 32
# This key is used to encrypt sessions and tokens
SECRET_KEY=your-secret-key-here-generate-with-openssl-rand-hex-32

# The public URL where Outline will be accessible
# Must match what users will type in their browser
OUTLINE_URL=http://localhost:3000

# Which port to expose Outline on (host machine)
OUTLINE_PORT=3000

# ============================================================
# Database Configuration
# ============================================================
# For SQLite (recommended for small installations):
# DATABASE_URL=sqlite:///data/outline.db
#
# SQLite is simple, requires no extra services, and works great
# for single-user or small team setups.
DATABASE_URL=sqlite:///data/outline.db

# ============================================================
# Redis Configuration
# ============================================================
# Redis is used for session caching and real-time features
REDIS_URL=redis://redis:6379
REDIS_PORT=6379

# ============================================================
# File Storage
# ============================================================
# Local filesystem storage (files stored in /var/lib/outline/data)
# This is the default and works well for most setups
# Files are persisted in the Docker volume: outline_data
FILE_STORAGE=local

# ============================================================
# SMTP Configuration (Optional)
# ============================================================
# Leave these blank to disable email notifications
# If you want email, fill in your SMTP server details

SMTP_HOST=
SMTP_PORT=
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@outline.local

# Example for Gmail:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-specific-password
# SMTP_FROM_EMAIL=your-email@gmail.com

# ============================================================
# Security
# ============================================================
# Force HTTPS in production (set to true if using HTTPS)
FORCE_HTTPS=false

# ============================================================
# Slack Integration (Optional)
# ============================================================
# Enable Slack message actions in Outline
SLACK_MESSAGE_ACTIONS=true

# ============================================================
# Google OAuth (Optional)
# ============================================================
# If you want to use Google for authentication, fill these in
# Leave blank to use default username/password auth

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ============================================================
# GitHub OAuth (Optional)
# ============================================================
# If you want to use GitHub for authentication, fill these in

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
